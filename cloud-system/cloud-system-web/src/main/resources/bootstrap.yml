spring:
  application:
    name: system-resources
  cloud:
    nacos:
      config:
        server-addr: ${nacos.server-addr}
        file-extension: yaml
      discovery:
        server-addr: ${nacos.server-addr}
server:
  port: 8080
#  servlet:
#    context-path: /sys
#    session:
#      cookie:
#        name: sysSESSION
nacos:
  server-addr: localhost:8848
